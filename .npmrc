# No dependency in this tree has (or needs) an install script, so refuse to
# run any — a compromised package can't execute code at install time.
# Explicit scripts (npm start, npm test) still run normally.
ignore-scripts=true
